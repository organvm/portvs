# Optional, incubator-scoped test/generator dependencies; observed execution versions.
# FFmpeg/ffprobe, system Chromium and DejaVu fonts are separate local prerequisites.
Pillow==12.3.0
playwright==1.57.0
